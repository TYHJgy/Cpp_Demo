

/*
C++中的 struct 和 class 基本是通用的，但是几个细节不同：
1）使用 class 时，类中的成员默认都是 private 属性的；而使用 struct 时，结构体中的成员默认都是 public 属性的。
2）class 继承默认是 private 继承，而 struct 继承默认是 public 继承。
3）class 可以使用模板，而 struct 不能。
*/
class TestStruct
{
private:
protected:
public:
	void startTest();		

};







